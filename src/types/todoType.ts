export type TodoType = {
    id: number;
    title: string;
    is_complete: boolean;
    priority_status: number;
};

export type PostTodoType = {
    title: string;
    is_complete: boolean;
    priority_status: number;
};